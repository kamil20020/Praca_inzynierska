\chapter{Podsumowanie}
\label{chap:podsumowanie}

Celem pracy by³o zaprojektowanie i zaimplementowanie aplikacji webowej IT Tech do gromadzenia i udostêpniania informacji o technologii programistycznych w postaci artyku³ów. U¿ytkownicy mieliby mo¿liwoœæ tworzenia, publikowania i przegl¹dania artyku³ów, jak i równie¿ mogliby zostawiaæ informacje zwrotne do danych artyku³ów w postaci komentarza lub opinii. Motywacj¹ przy wyborze implementacji takiego systemu by³y brak popularnych serwisów informacyjnych zorientowanych jedynie na technologiach programistycznych oraz istnienie wielu wad wœród istniej¹cych podobnych rozwi¹zañ. Do tych wad mo¿na zaliczyæ niewystarczaj¹co rozbudowan¹ formê spo³ecznoœciow¹, czy zbyt ogólne albo zbyt szczegó³owe zamieszczane treœci. We wstêpie pracy zosta³a równie¿ zawarta analiza i ocena dwóch popularnych serwisów Wikipedia oraz Medium, które s¹ zorientowane na wiele dziedzin.

Zakres pracy obejmowa³ projekt, implementacjê, testowanie oraz wdro¿enie i opracowanie dokumentacji. W ramach projektu systemu IT Tech na pocz¹tku okreœlono wymagania funkcjonalne i niefunkcjonalne. Na podstawie tych wymagañ opracowano architekturê systemu. Realizowany system by³ rozproszon¹ aplikacj¹ webow¹, w której komunikacja odbywa³a siê poprzez REST oraz mo¿na by³o wyró¿niæ dwie bazy danych SQL i NoSQL. Na podstawie wymagañ oraz architektury systemu opracowano przypadki u¿ycia wraz z ich szczegó³owymi opisami oraz zaprojektowano odpowiadaj¹ce im prototypy interfejsu u¿ytkownika. W ramach implementacji stworzono aplikacjê serwerow¹ w frameworku Spring, aplikacjê klienck¹ w bibliotece React oraz warstwê bezpieczeñstwa jako Keycloak. Dzia³anie aplikacji serwerowej zosta³o zweryfikowane poprzez testy integracyjne kontrolerów.
Na koñcu aplikacja zosta³a wdro¿ona przy u¿yciu platform Azure oraz MongoDB Cloud.

Aplikacjê uda³o siê zrealizowaæ prawie w ca³oœci. Z powodu niewystarczaj¹cego czasu nie uda³o siê zrealizowaæ zarz¹dzania technologiami przez recenzenta oraz nie przetestowano aplikacji wystarczaj¹co. Na pocz¹tku planowano wykonaæ testy integracyjne kontrolerów backendu w ca³oœci oraz dla du¿ej czêœci funkcji frontendu. Przetestowano jedynie backend, ale i tak pokrycie tych testów by³o mniejsze ni¿ 50\%.

Podczas realizacji aplikacji najwiêcej trudnoœci mi przysporzy³y u¿ycie technologii Keycloak bez wymuszonego przekierowania do formularza logowania dostarczanego przez ten serwer oraz u¿ycie dwóch ró¿nego typu baz danych w jednym projekcie Spring. Problemy te wynika³y g³ównie z braku doœwiadczenia. 

Bardzo du¿ym wsparciem by³ promotor dr in¿. Pawe³ Rogaliñski, który by³ niezwykle zaanga¿owany w realizacjê tej pracy. Dziêki pracy nad tym projektem uda³o siê zdobyæ du¿o doœwiadczenia z zakresu projektowania systemów informatycznych, implementacji aplikacji z wykorzystaniem technologii webowych, przeprowadzaniu testów jednostkowych oraz wdro¿enia systemów na popularnych platformach chmurowych.