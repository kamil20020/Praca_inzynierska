\chapter{Opis za³¹czonej p³yty CD/DVD}

Na do³¹czonej do tej pracy p³ycie CD znajduj¹ siê wersja elektroniczna pracy o nazwie \texttt{W04N\_254049\_2022\_praca\_inzynierska.pdf} oraz kod aplikacji w folderze o nazwie \texttt{aplikacja}. Kod aplikacji podzielony jest na 3 foldery: \texttt{frontend}, \texttt{backend} oraz \texttt{keycloak}. W folderze \texttt{frontend} znajduje siê kod aplikacji klienckiej. W folderze \texttt{backed} jest zawarty kod aplikacji serwerowej. W folderze \texttt{keycloak} przechowywany jest serwer Keycloak w wersji samodzielnej (ang.\ \emph{standalone}).